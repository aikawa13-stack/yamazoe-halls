rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All application data is accessed through trusted server-side functions
    // until product-specific client authorization rules are implemented.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
