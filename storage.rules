rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Keep Storage private until product-specific client authorization rules
    // and path constraints are implemented.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
